jQuery(window).load(function(){

	jQuery( "#ms_admin_tabs" ).tabs();
/*	var tabs = jQuery( "#hompage-tabs" ).tabs();
	tabs.find( ".ui-tabs-nav" ).sortable({
		axis: "x",
		stop: function() {
			tabs.tabs( "refresh" );
		}
	});
*/
});
